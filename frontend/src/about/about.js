import React, {Fragment} from 'react';

import Header from '../template/header';

export default props => (
    <Fragment>
        <Header name='Sobre' description='nós'></Header>
    </Fragment>
);
